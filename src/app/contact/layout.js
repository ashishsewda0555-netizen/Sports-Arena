export const metadata = {
  title: 'Contact Us',
  description: 'Get in touch with Bharti Sports Arena via call, WhatsApp, or visit us in Sikar. Find directions and operating hours.',
};

export default function ContactLayout({ children }) {
  return children;
}
